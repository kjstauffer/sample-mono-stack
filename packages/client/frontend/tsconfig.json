{
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    "types": [
      /* Not merged with root tsconfig... Must include full list */
      "node",
      "jest",
      "@testing-library/jest-dom",
    ]
  },
  "include": [
    /* Any additions to this list need to also go into `tsconfig.eslint.json` */
    "codegen.ts",
    "src/**/*",
    "scripts/**/*"
  ],
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}
